{
  "name": "dropzone",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "format": "prettier --write src styles *.html",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "0.160.0"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "vite": "^6.4.1"
  }
}
